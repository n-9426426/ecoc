<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.vehicle.mapper.VehicleInfoMapper">

	<resultMap type="VehicleInfo" id="VehicleInfoResult">
		<result property="vehicleId"          column="vehicle_id"           />
		<result property="vin"                column="vin"                  />
		<result property="vehicleModel"       column="vehicle_model"        />
		<result property="factoryCode"        column="factory_code"         />
		<result property="factoryName"        column="factory_name"         />
		<result property="materialNo"         column="material_no"          />
		<result property="uploadStatus"       column="upload_status"        />
		<result property="validationResult"   column="validation_result"    />
		<result property="status"             column="status"               />
		<result property="country"            column="country"              />
		<result property="color"              column="color"                />
		<result property="secondaryColor"     column="secondary_color"      />
		<result property="certificateVersion" column="certificate_version"  />
		<result property="wvtaNo"             column="wvta_no"              />
		<result property="manufactureDate"    column="manufacture_date"     />
		<result property="issueDate"          column="issue_date"           />
		<result property="json"               column="json"                 />
		<result property="xmlTemplateId"      column="xml_template_id"      />
		<result property="vehicleTemplateId"  column="vehicle_template_id"  />
		<result property="deleted"            column="deleted"              />
		<result property="createBy"           column="create_by"            />
		<result property="createTime"         column="create_time"          />
		<result property="updateBy"           column="update_by"            />
		<result property="updateTime"         column="update_time"          />
		<result property="remark"             column="remark"               />
		<result property="vehicleModelStr"    column="vehicle_model_str"    />
		<result property="vehicleTemplateFilepath" column="vehicle_template_filepath"/>
		<result property="cocTemplateNo"      column="coc_template_no"      />
		<result property="validationReportJson" column="validation_report_json"/>
		<result property="engineNumber" column="engine_number"/>
		<result property="batteryNumber" column="battery_number"/>
		<result property="motorNumber" column="motor_number"/>
		<result property="brand" column="brand"/>
		<result property="weight" column="weight"/>
		<result property="saleName" column="sale_name"/>
		<result property="tire" column="tire"/>
		<result property="tvv" column="tvv"/>
		<result property="breakpointTime" column="breakpoint_time"/>
		<result property="version" column="version"/>
		<result property="tempVersion" column="temp_version"/>
		<result property="generateAffirm"       column="generate_affirm"        />
		<result property="uploadAffirm"         column="upload_affirm"          />
		<result property="projectName"          column="project_name"           />
		<result property="customerNo"           column="customer_no"            />
		<result property="tireResistanceGrade"  column="tire_resistance_grade"  />
		<result property="firstMaterialFlag"      column="first_material_flag"      />
		<result property="firstTemplateFlag"      column="first_template_flag"      />
		<result property="materialConfirmedBy"    column="material_confirmed_by"    />
		<result property="materialConfirmedTime"  column="material_confirmed_time"  />
		<result property="templateConfirmedBy"    column="template_confirmed_by"    />
		<result property="templateConfirmedTime"  column="template_confirmed_time"  />
		<result property="generateAffirmNotice" column="generate_affirm_notice" />
		<result property="uploadAffirmNotice"   column="upload_affirm_notice"   />
		<result property="saleCompanyName"      column="sale_company_name"      />
		<result property="modelNo"              column="model_no"               />
		<result property="affirmCause"          column="affirm_cause"           />
		<result property="hasGeneratedXml"      column="has_generated_xml"       />
		<result property="xmlFileId"            column="xml_file_id"             />
		<result property="xmlValidateResult"    column="xml_validate_result"     />
		<result property="forceUploaded"        column="force_uploaded"          />
	</resultMap>

	<sql id="selectVehicleInfoVo">
		select vi.vehicle_id, vi.vin, vi.factory_code, vi.factory_name, vi.material_no, vi.vehicle_model,
			   vi.upload_status, vi.validation_result, vi.status, country, vi.color, vi.secondary_color, vi.temp_version,
			   vi.certificate_version, vi.wvta_no, vi.manufacture_date, vi.issue_date, vi.json, vi.xml_template_id, vi.vehicle_template_id,
			   vi.deleted, vi.create_by, vi.create_time, vi.update_by, vi.update_time, vi.remark, vi.validation_report_json, vt.version as version,
			   vi.engine_number, vi.motor_number, vi.brand, vi.weight, vi.sale_name, vi.tire, vi.battery_number, vi.tvv, vi.breakpoint_time,
			   sdd.dict_label as vehicle_model_str, vt.filepath as vehicle_template_filepath, vt.coc_template_no as coc_template_no,
			   vi.generate_affirm, vi.upload_affirm, vi.project_name, vi.customer_no, vi.tire_resistance_grade,   vi.first_material_flag,
			   vi.first_template_flag, vi.material_confirmed_by, vi.material_confirmed_time, vi.template_confirmed_by, vi.template_confirmed_time,
			   vi.generate_affirm_notice, vi.upload_affirm_notice, vi.sale_company_name, vi.model_no, vi.affirm_cause
		from vehicle_info vi
				 left join (select dict_code, dict_label from sys_dict_data where dict_type = 'vehicle_model') sdd on vi.vehicle_model = sdd.dict_code
				 left join (select template_id, filepath, coc_template_no, is_last, version, model_no, tvv from vehicle_template) vt on vi.vehicle_template_id = vt.template_id
	</sql>

	<select id="selectVehicleInfoById" parameterType="Long" resultMap="VehicleInfoResult">
		<include refid="selectVehicleInfoVo"/>
		where vehicle_id = #{vehicleId}
	</select>

	<select id="selectVehicleInfoList" parameterType="VehicleInfo" resultMap="VehicleInfoResult">
		<include refid="selectVehicleInfoVo"/>
		<where>
			and 1 = 1
			<if test="affirm != null">
				<choose>
					<when test="affirm == 1">
						AND vi.generate_affirm = 1
						AND vi.upload_affirm = 1
					</when>
					<when test="affirm == 0">
						AND (vi.generate_affirm = 0 OR vi.upload_affirm = 0)
					</when>
				</choose>
			</if>
			<if test="vehicleIds != null and vehicleIds.size() > 0">
				and vehicle_id in
				<foreach item="vehicleId" collection="vehicleIds" open="(" separator="," close=")">
					#{vehicleId}
				</foreach>
			</if>
			<if test="vinList != null and vinList.size() > 0">
				AND (
				<foreach collection="vinList" item="v" separator=" OR ">
					vin LIKE CONCAT('%', #{v}, '%')
				</foreach>
				)
			</if>
			<if test="cocTemplateNo != null and cocTemplateNo != ''">
				and vt.coc_template_no like CONCAT('%', #{cocTemplateNo}, '%')
			</if>
			<if test="tvv != null and tvv != ''">
				and vi.tvv like concat('%', #{tvv}, '%')
			</if>
			<if test="materialNoList != null and materialNoList.size() > 0">
				AND (
				<foreach collection="materialNoList" item="m" separator=" OR ">
					material_no LIKE CONCAT('%', #{m}, '%')
				</foreach>
				)
			</if>
			<if test="projectName != null and projectName != ''">
				and project_name LIKE CONCAT('%', #{projectName}, '%')
			</if>
			<if test="modelNo != null and modelNo != ''">
				and vt.model_no LIKE CONCAT('%', #{modelNo}, '%')
			</if>
			<if test="vehicleModelList != null and vehicleModelList.size() > 0">
				AND (
				<foreach collection="vehicleModelList" item="m" separator=" OR ">
					vehicle_model LIKE CONCAT('%', #{m}, '%')
				</foreach>
				)
			</if>
			<if test="factoryCodes != null and factoryCodes.size() > 0">
				and factory_code in
				<foreach item="factoryCode" collection="factoryCodes" open="(" separator="," close=")">
					#{factoryCode}
				</foreach>
			</if>
			<if test="color != null and color != ''">
				AND color = #{color}
			</if>
			<if test="colors != null and colors.size() > 0">
				AND color in
				<foreach item="color" collection="colors" open="(" separator="," close=")">
					#{color}
				</foreach>
			</if>
			<if test="secondaryColor != null and secondaryColor != ''">
				AND secondary_color like concat('%', #{secondaryColor}, '%')
			</if>
			<if test="secondaryColors != null and secondaryColors.size() > 0">
				AND color in
				<foreach item="secondaryColor" collection="secondaryColors" open="(" separator="," close=")">
					#{secondaryColor}
				</foreach>
			</if>
			<if test="countries != null and countries.size() > 0">
				AND country in
				<foreach item="country" collection="countries" open="(" separator="," close=")">
					#{country}
				</foreach>
			</if>
			<if test="manufactureBeginTime != null">
				AND manufacture_date >= #{manufactureBeginTime}
			</if>
			<if test="manufactureEndTime != null">
				AND manufacture_date &lt;= #{manufactureEndTime}
			</if>
			<if test="issueDateBeginTime != null">
				AND issue_date &gt;= #{issueDateBeginTime}
			</if>
			<if test="issueDateEndTime != null">
				AND issue_date &lt;= #{issueDateEndTime}
			</if>
			<if test="engineNumber != null and engineNumber != ''">
				and engine_number like concat('%', #{engineNumber}, '%')
			</if>
			<if test="motorNumber != null and motorNumber != ''">
				and motor_number like concat('%', #{motorNumber}, '%')
			</if>
			<if test="tire != null and tire != ''">
				and tire like concat('%', #{tire}, '%')
			</if>
			<if test="brand != null and brand != ''">
				and brand like concat('%', #{brand}, '%')
			</if>
			<if test="weight != null and weight != ''">
				and weight like concat('%', #{weight}, '%')
			</if>
			<if test="saleName != null and saleName != ''">
				and sale_name like concat('%', #{saleName}, '%')
			</if>
			<if test="tireResistanceGrade != null and tireResistanceGrade != ''">
				and tire_resistance_grade like concat('%', #{tireResistanceGrade}, '%')
			</if>
			<if test="saleCompanyName != null and saleCompanyName != ''">
				and sale_company_name like concat('%', #{saleCompanyName}, '%')
			</if>
			<if test="modelNo != null and modelNo != ''">
				and vi.model_no like concat('%', #{modelNo}, '%')
			</if>
			<if test="customerNo != null and customerNo != ''">
				and customer_no like concat('%', #{customerNo}, '%')
			</if>
			<if test="status != null">
				AND status = #{status}
			</if>
			<if test="validationResult != null">
				AND validation_result = #{validationResult}
			</if>
			<if test="validationResults != null and validationResults.size() > 0">
				AND validation_result in
				<foreach item="validationResult" collection="validationResults" open="(" separator="," close=")">
					#{validationResult}
				</foreach>
			</if>
			<if test="uploadStatusList != null and uploadStatusList.size() > 0">
				AND upload_status in
				<foreach item="uploadStatus" collection="uploadStatusList" open="(" separator="," close=")">
					#{uploadStatus}
				</foreach>
			</if>
			<if test="generateAffirm != null">
				and generate_affirm = #{generateAffirm}
			</if>
			<if test="uploadAffirm != null">
				and upload_affirm = #{uploadAffirm}
			</if>
			<!--			<if test="createdBeginTime != null">-->
			<!--				AND date_format(create_time,'%y%m%d') &gt;= date_format(#{createdBeginTime},'%y%m%d')-->
			<!--			</if>-->
			<!--			<if test="createdEndTime != null">-->
			<!--				AND date_format(create_time,'%y%m%d') &lt;= date_format(#{createdEndTime},'%y%m%d')-->
			<!--			</if>-->
			<!--			<if test="factoryName != null and factoryName != ''">-->
			<!--				AND factory_name like concat('%', #{factoryName}, '%')-->
			<!--			</if>-->
			<if test="wvtaNo != null and wvtaNo != ''">
				AND wvta_no like concat('%', #{wvtaNo}, '%')
			</if>
			<if test="cocTemplateNo != null and cocTemplateNo != ''">
				AND coc_template_no like concat('%', #{cocTemplateNo}, '%')
			</if>
			<if test="reclaim != null and reclaim">
				AND deleted = 2
			</if>
			<if test="reclaim == null or !reclaim">
				AND deleted = 0
			</if>
			<if test="breakpointTime != null">
				AND DATE(breakpoint_time) = DATE(#{breakpointTime})
			</if>
			<if test="firstMaterialFlag != null">
				AND vi.first_material_flag = #{firstMaterialFlag}
				AND vi.generate_affirm = #{firstMaterialFlag}
			</if>
			<if test="firstTemplateFlag != null">
				AND vi.first_template_flag = #{firstTemplateFlag}
				AND vi.upload_affirm = #{firstTemplateFlag}
			</if>
			${params.dataScope}
		</where>
		order by update_time desc
	</select>

	<insert id="insertVehicleInfo" parameterType="VehicleInfo" useGeneratedKeys="true" keyProperty="vehicleId">
		insert into vehicle_info (
		<if test="vin != null and vin != ''">vin, </if>
		<if test="vehicleModel != null">vehicle_model, </if>
		<if test="factoryCode != null and factoryCode != ''">factory_code, </if>
		<if test="factoryName != null and factoryName != ''">factory_name, </if>
		<if test="materialNo != null and materialNo != ''">material_no, </if>
		<if test="uploadStatus != null">upload_status, </if>
		<if test="validationResult != null">validation_result, </if>
		<if test="status != null">status, </if>
		<if test="country != null and country != ''">country, </if>
		<if test="color != null and color != ''">color, </if>
		<if test="secondaryColor != null and secondaryColor != ''">secondary_color, </if>
		<if test="certificateVersion != null and certificateVersion != ''">certificate_version, </if>
		<if test="wvtaNo != null and wvtaNo != ''">wvta_no, </if>
		<if test="manufactureDate != null">manufacture_date,</if>
		<if test="issueDate != null">issue_date, </if>
		<if test="json != null and json != ''">json, </if>
		<if test="xmlTemplateId != null">xml_template_id, </if>
		<if test="vehicleTemplateId != null and vehicleTemplateId != ''">vehicle_template_id, </if>
		<if test="remark != null and remark != ''">remark, </if>
		<if test="validationReportJson != null and validationReportJson != ''">validation_report_json,</if>
		<if test="engineNumber != null and engineNumber != ''">engine_number,</if>
		<if test="batteryNumber != null and batteryNumber != ''">battery_number,</if>
		<if test="motorNumber != null and motorNumber != ''">motor_number,</if>
		<if test="brand != null and brand != ''">brand,</if>
		<if test="weight != null and weight != ''">weight,</if>
		<if test="saleName != null and saleName != ''">sale_name,</if>
		<if test="saleCompanyName != null and saleCompanyName != ''">sale_company_name,</if>
		<if test="tire != null and tire != ''">tire,</if>
		<if test="tvv != null and tvv != ''">tvv,</if>
		<if test="breakpointTime != null">breakpoint_time,</if>
		<if test="generateAffirm != null">generate_affirm,</if>
		<if test="uploadAffirm != null">upload_affirm,</if>
		<if test="projectName != null and projectName != ''">project_name,</if>
		<if test="customerNo != null and customerNo != ''">customer_no,</if>
		<if test="tireResistanceGrade != null and tireResistanceGrade != ''">tire_resistance_grade,</if>
		<if test="createBy != null and createBy != ''">create_by, </if>
		<if test="generateAffirmNotice != null">generate_affirm_notice,</if>
		<if test="uploadAffirmNotice != null">upload_affirm_notice,</if>
		<if test="modelNo != null and modelNo != ''">model_no,</if>
		<if test="affirmCause != null and affirmCause != ''">affirm_cause,</if>
		<if test="firstMaterialFlag != null">first_material_flag,</if>
		<if test="firstTemplateFlag != null">first_template_flag,</if>
		create_time
		) values (
		<if test="vin != null and vin != ''">#{vin}, </if>
		<if test="vehicleModel != null">#{vehicleModel}, </if>
		<if test="factoryCode != null and factoryCode != ''">#{factoryCode}, </if>
		<if test="factoryName != null and factoryName != ''">#{factoryName}, </if>
		<if test="materialNo != null and materialNo != ''">#{materialNo}, </if>
		<if test="uploadStatus != null">#{uploadStatus}, </if>
		<if test="validationResult != null">#{validationResult}, </if>
		<if test="status != null">#{status}, </if>
		<if test="country != null and country != ''">#{country}, </if>
		<if test="color != null and color != ''">#{color}, </if>
		<if test="secondaryColor != null and secondaryColor != ''">#{secondaryColor}, </if>
		<if test="certificateVersion != null and certificateVersion != ''">#{certificateVersion}, </if>
		<if test="wvtaNo != null and wvtaNo != ''">#{wvtaNo}, </if>
		<if test="manufactureDate != null">#{manufactureDate},</if>
		<if test="issueDate != null">#{issueDate}, </if>
		<if test="json != null and json != ''">#{json}, </if>
		<if test="xmlTemplateId != null">#{xmlTemplateId}, </if>
		<if test="vehicleTemplateId != null and vehicleTemplateId != ''">#{vehicleTemplateId}, </if>
		<if test="remark != null and remark != ''">#{remark}, </if>
		<if test="validationReportJson != null and validationReportJson != ''">#{validationReportJson},</if>
		<if test="engineNumber != null and engineNumber != ''">#{engineNumber},</if>
		<if test="batteryNumber != null and batteryNumber != ''">#{batteryNumber},</if>
		<if test="motorNumber != null and motorNumber != ''">#{motorNumber},</if>
		<if test="brand != null and brand != ''">#{brand},</if>
		<if test="weight != null and weight != ''">#{weight},</if>
		<if test="saleName != null and saleName != ''">#{saleName},</if>
		<if test="saleCompanyName != null and saleCompanyName != ''">#{saleCompanyName},</if>
		<if test="tire != null and tire != ''">#{tire},</if>
		<if test="tvv != null and tvv != ''">#{tvv},</if>
		<if test="breakpointTime != null">#{breakpointTime},</if>
		<if test="generateAffirm != null">#{generateAffirm},</if>
		<if test="uploadAffirm != null">#{uploadAffirm},</if>
		<if test="projectName != null and projectName != ''">#{projectName},</if>
		<if test="customerNo != null and customerNo != ''">#{customerNo},</if>
		<if test="tireResistanceGrade != null and tireResistanceGrade != ''">#{tireResistanceGrade},</if>
		<if test="createBy != null and createBy != ''">#{createBy}, </if>
		<if test="generateAffirmNotice != null">#{generateAffirmNotice},</if>
		<if test="uploadAffirmNotice != null">#{uploadAffirmNotice},</if>
		<if test="modelNo != null and modelNo != ''">#{modelNo},</if>
		<if test="affirmCause != null and affirmCause != ''">#{affirmCause},</if>
		<if test="firstMaterialFlag != null">#{firstMaterialFlag},</if>
		<if test="firstTemplateFlag != null">#{firstTemplateFlag},</if>
		sysdate()
		)
	</insert>

	<update id="updateVehicleInfo" parameterType="VehicleInfo">
		update vehicle_info
		<set>
			<if test="vin != null and vin != ''">vin = #{vin}, </if>
			<if test="vehicleModel != null">vehicle_model = #{vehicleModel}, </if>
			<if test="factoryCode != null and factoryCode != ''">factory_code = #{factoryCode}, </if>
			<if test="factoryName != null and factoryName != ''">factory_name = #{factoryName}, </if>
			<if test="materialNo != null and materialNo != ''">material_no = #{materialNo}, </if>
			<if test="uploadStatus != null">upload_status = #{uploadStatus}, </if>
			<if test="validationResult != null">validation_result = #{validationResult}, </if>
			<if test="status != null">status = #{status}, </if>
			<if test="country != null and country != ''">country = #{country}, </if>
			<if test="color != null and color != ''">color = #{color}, </if>
			<if test="secondaryColor != null and secondaryColor != ''">secondary_color = #{secondaryColor}, </if>
			<if test="certificateVersion != null and certificateVersion != ''">certificate_version = #{certificateVersion}, </if>
			<if test="wvtaNo != null and wvtaNo != ''">wvta_no = #{wvtaNo}, </if>
			<if test="manufactureDate != null">manufacture_date = #{manufactureDate},</if>
			<if test="issueDate != null">issue_date = #{issueDate}, </if>
			<if test="json != null">json = #{json}, </if>
			<if test="xmlTemplateId != null">xml_template_id = #{xmlTemplateId}, </if>
			<if test="vehicleTemplateId != null and vehicleTemplateId != ''">vehicle_template_id = #{vehicleTemplateId}, </if>
			<if test="remark != null">remark = #{remark}, </if>
			<if test="validationReportJson != null and validationReportJson != ''">validation_report_json = #{validationReportJson},</if>
			<if test="engineNumber != null and engineNumber != ''">engine_number = #{engineNumber},</if>
			<if test="batteryNumber != null and batteryNumber != ''">battery_number = #{batteryNumber},</if>
			<if test="motorNumber != null and motorNumber != ''">motor_number = #{motorNumber},</if>
			<if test="updateBy != null and updateBy != ''">update_by = #{updateBy}, </if>
			<if test="deleted != null">deleted = #{deleted}, </if>
			<if test="brand != null and brand != ''">brand = #{brand},</if>
			<if test="weight != null and weight != ''">weight = #{weight},</if>
			<if test="saleName != null and saleName != ''">sale_name = #{saleName},</if>
			<if test="saleCompanyName != null and saleCompanyName != ''">sale_company_name = #{saleCompanyName},</if>
			<if test="tire != null and tire != ''">tire = #{tire},</if>
			<if test="tvv != null and tvv != ''">tvv = #{tvv},</if>
			<if test="breakpointTime != null">breakpoint_time = #{breakpointTime},</if>
			<if test="generateAffirm != null">generate_affirm = #{generateAffirm},</if>
			<if test="uploadAffirm != null">upload_affirm = #{uploadAffirm},</if>
			<if test="projectName != null and projectName != ''">project_name = #{projectName},</if>
			<if test="customerNo != null and customerNo != ''">customer_no = #{customerNo},</if>
			<if test="tireResistanceGrade != null and tireResistanceGrade != ''">tire_resistance_grade = #{tireResistanceGrade},</if>
			<if test="generateAffirmNotice != null">generate_affirm_notice = #{generateAffirmNotice},</if>
			<if test="uploadAffirmNotice != null">upload_affirm_notice = #{uploadAffirmNotice},</if>
			<if test="modelNo != null and modelNo != ''">model_no = #{modelNo},</if>
			<if test="affirmCause != null and affirmCause != ''">affirm_cause = #{affirmCause},</if>
			<if test="firstMaterialFlag != null">first_material_flag = #{firstMaterialFlag},</if>
			<if test="firstTemplateFlag != null">first_template_flag = #{firstTemplateFlag},</if>
			update_time = sysdate()
		</set>
		where vehicle_id = #{vehicleId}
	</update>

	<update id="deleteVehicleInfoByIds" parameterType="String">
		update vehicle_info set deleted = 2 where vehicle_id in
		<foreach item="vehicleId" collection="array" open="(" separator="," close=")">
			#{vehicleId}
		</foreach>
	</update>

	<update id="restoreVehicleInfoByIds" parameterType="String">
		update vehicle_info set deleted = 0 where vehicle_id in
		<foreach item="vehicleId" collection="array" open="(" separator="," close=")">
			#{vehicleId}
		</foreach>
	</update>

	<delete id="permanentlyDeleteVehicleInfoByIds" parameterType="Long">
		delete from vehicle_info where deleted = 2 and vehicle_id in
		<foreach item="vehicleId" collection="array" open="(" separator="," close=")">
			#{vehicleId}
		</foreach>
	</delete>

	<delete id="permanentlyDeleteVehicleInfoById" parameterType="Long">
		delete from vehicle_info where deleted = 2 and vehicle_id = #{vehicleId}
	</delete>

	<delete id="deleteExpiredVehicleInfo">
		delete from vehicle_info
		where deleted = 2
		  and update_time &lt; date_sub(now(), interval 1 month)
	</delete>

	<select id="selectVehicleInfoByWvtaNo" parameterType="String" resultMap="VehicleInfoResult">
		<include refid="selectVehicleInfoVo"/>
		where wvta_no = #{wvtaNo}
		and deleted = 0
	</select>

	<select id="selectVehicleInfoByVin" parameterType="String" resultMap="VehicleInfoResult">
		<include refid="selectVehicleInfoVo"/>
		where vin = #{vin}
		and deleted = 0
	</select>
	<select id="selectByVinAndDeleted" resultMap="VehicleInfoResult">
		SELECT * FROM vehicle_info
		WHERE vin = #{vin} AND deleted = #{deleted}
			LIMIT 1
	</select>

	<update id="updateStatus">
		UPDATE vehicle_info
		SET
			status      = #{status},
			update_by   = #{updateBy},
			update_time = SYSDATE()
		WHERE vehicle_id = #{vehicleId}
	</update>

	<select id="selectVehicleInfoByIds" resultMap="VehicleInfoResult">
		SELECT * FROM vehicle_info WHERE vehicle_id IN
		<foreach item="vehicleId" collection="array" open="(" separator="," close=")">
			#{vehicleId}
		</foreach>
	</select>

	<select id="checkOldTemplate" resultMap="VehicleInfoResult">
		<include refid="selectVehicleInfoVo"/>
		where vt.is_last = 0 and vi.old_template_notice = 0 and vi.deleted = 0
	</select>

	<update id="updateVehicleInfoOldTemplate">
		UPDATE vehicle_info
		SET old_template_notice = #{status},
		update_time = NOW()
		WHERE vehicle_id IN
		<foreach collection="vehicleIds" item="id" open="(" separator="," close=")">
			#{id}
		</foreach>
		AND deleted = 0
	</update>

	<update id="updateVehicleInfoGenerateTimeout">
		UPDATE vehicle_info
		SET generate_timeout_notice = #{status},
		update_time = NOW()
		WHERE vehicle_id IN
		<foreach collection="vehicleIds" item="id" open="(" separator="," close=")">
			#{id}
		</foreach>
		AND deleted = 0
	</update>

	<select id="selectVehicleInfoByVinManufactureDate" parameterType="String" resultMap="VehicleInfoResult">
		<include refid="selectVehicleInfoVo"/>
		where vi.deleted = 0
		and vi.manufacture_date >= (select manufacture_date from vehicle_info where vin = #{vin} and deleted = 0 limit 1)
	</select>

	<update id="updateTempVersionByVin">
		UPDATE vehicle_info
		SET temp_version = #{tempVersion},
			update_time = SYSDATE()
		WHERE vin = #{vin}
	</update>

	<update id="updateVehicleTemplateIdByTempVersion">
		UPDATE vehicle_info vi
		JOIN vehicle_template vt
		ON vt.uuid = (SELECT uuid FROM vehicle_template WHERE template_id = vi.vehicle_template_id LIMIT 1)
		AND vt.version = vi.temp_version
		SET vi.vehicle_template_id = vt.template_id,
		vi.json = vt.json,
		vi.temp_version = NULL
		WHERE vi.temp_version IS NOT NULL
		AND vi.deleted = 0
		AND <choose>
		<when test="list != null and list.size() > 0">
			vi.vin IN
			<foreach collection="list" item="vin" open="(" separator="," close=")">
				#{vin}
			</foreach>
		</when>
		<otherwise>
			vi.manufacture_date >= (
			SELECT min_date FROM (
			SELECT vi2.manufacture_date AS min_date
			FROM vehicle_info vi2
			WHERE vi2.vin = (SELECT vin FROM breakpoint WHERE id = #{breakpointId} LIMIT 1)
			AND vi2.deleted = 0
			LIMIT 1
			) tmp
			)
		</otherwise>
	</choose>
	</update>

	<!-- ==================== 首台车管理 ==================== -->

	<!-- 查询该物料号下制造时间最早的 vehicle_id -->
	<select id="findEarliestIdByMaterialNo" resultType="long">
		SELECT vehicle_id
		FROM vehicle_info
		WHERE material_no = #{materialNo}
		  AND deleted = 0
		ORDER BY manufacture_date ASC
			LIMIT 1
	</select>

	<!-- 查询该模版下制造时间最早的 vehicle_id -->
	<select id="findEarliestIdByTemplateId" resultType="long">
		SELECT vehicle_id
		FROM vehicle_info
		WHERE vehicle_template_id = #{templateId}
		  AND deleted = 0
		ORDER BY manufacture_date ASC
			LIMIT 1
	</select>

	<!-- 该物料号下是否存在已确认记录（generate_affirm = 1） -->
	<!-- 修改说明：原来判断 material_confirmed_time IS NOT NULL，                 -->
	<!--           现改为判断 generate_affirm = 1，语义更清晰，                  -->
	<!--           与 confirmMaterialFlag 的写入字段保持一致                      -->
	<select id="existsConfirmedMaterial" resultType="boolean">
		SELECT COUNT(1) > 0
		FROM vehicle_info
		WHERE material_no = #{materialNo}
		  AND generate_affirm = 1
		  AND deleted = 0
	</select>

	<!-- 该模版下是否存在已确认记录（upload_affirm = 1） -->
	<select id="existsConfirmedTemplate" resultType="boolean">
		SELECT COUNT(1) > 0
		FROM vehicle_info
		WHERE vehicle_template_id = #{templateId}
		  AND upload_affirm = 1
		  AND deleted = 0
	</select>

	<!-- 清除该物料号下所有车辆的物料号首台标识 -->
	<update id="clearMaterialFlagByMaterialNo">
		UPDATE vehicle_info
		SET first_material_flag = 0,
			update_time         = SYSDATE()
		WHERE material_no = #{materialNo}
		  AND deleted = 0
	</update>

	<!-- 清除该模版下所有车辆的模版首台标识 -->
	<update id="clearTemplateFlagByTemplateId">
		UPDATE vehicle_info
		SET first_template_flag = 0,
			update_time         = SYSDATE()
		WHERE vehicle_template_id = #{templateId}
		  AND deleted = 0
	</update>

	<!-- 打物料号首台标识 -->
	<!-- 打物料号首台标识，同时写入确认原因 -->
	<update id="markMaterialFlag">
		UPDATE vehicle_info
		SET first_material_flag = #{flag},
			affirm_cause        = CASE WHEN #{flag} = 1 THEN '物料号新增' ELSE affirm_cause END,
			update_time         = SYSDATE()
		WHERE vehicle_id = #{vehicleId}
	</update>

	<!-- 打模版首台标识，同时写入确认原因 -->
	<update id="markTemplateFlag">
		UPDATE vehicle_info
		SET first_template_flag = #{flag},
			affirm_cause        = CASE WHEN #{flag} = 1
										   THEN CASE
													WHEN affirm_cause IS NOT NULL AND affirm_cause != ''
                                                THEN CONCAT(affirm_cause, '、模版新增')
													ELSE '模版新增'
					END
									   ELSE affirm_cause
				END,
			update_time         = SYSDATE()
		WHERE vehicle_id = #{vehicleId}
	</update>

	<update id="confirmMaterialFlag">
		UPDATE vehicle_info
		SET generate_affirm         = CASE WHEN generate_affirm = 1 THEN 0 ELSE 1 END,
			material_confirmed_by   = #{confirmedBy},
			material_confirmed_time = SYSDATE(),
			update_by               = #{confirmedBy},
			update_time             = SYSDATE()
		WHERE vehicle_id = #{vehicleId}
	</update>

	<update id="confirmTemplateFlag">
		UPDATE vehicle_info
		SET upload_affirm           = CASE WHEN upload_affirm = 1 THEN 0 ELSE 1 END,
			template_confirmed_by   = #{confirmedBy},
			template_confirmed_time = SYSDATE(),
			update_by               = #{confirmedBy},
			update_time             = SYSDATE()
		WHERE vehicle_id = #{vehicleId}
	</update>

	<!-- 重置该物料号下所有车辆的物料号确认状态（预留，暂未使用） -->
	<update id="resetMaterialConfirm">
		UPDATE vehicle_info
		SET generate_affirm         = 0,
			material_confirmed_by   = NULL,
			material_confirmed_time = NULL,
			update_time             = SYSDATE()
		WHERE material_no = #{materialNo}
		  AND deleted = 0
	</update>

	<!-- 重置该模版下所有车辆的模版确认状态（模版改动后调用） -->
	<update id="resetTemplateConfirm">
		UPDATE vehicle_info
		SET upload_affirm           = 0,
			template_confirmed_by   = NULL,
			template_confirmed_time = NULL,
			update_time             = SYSDATE()
		WHERE vehicle_template_id = #{templateId}
		  AND deleted = 0
		  AND upload_affirm = 0  -- ← 只重置未确认的，已确认的不动
	</update>

	<!--
        首台车待确认列表（物料号维度 / 模版维度统一查询）
        dimension = 'material' : first_material_flag=0 AND generate_affirm=0
        dimension = 'template' : first_template_flag=0 AND upload_affirm=0

        其余字段复用 vehicleInfo 对象的查询条件，与 selectVehicleInfoList 保持一致
    -->
	<select id="listFirstVehicleUnconfirmed" resultMap="VehicleInfoResult">
		SELECT vi.vehicle_id, vi.vin, vi.factory_code, vi.factory_name, vi.material_no, vi.vehicle_model,
		vi.upload_status, vi.validation_result, vi.status, vi.country, vi.color, vi.secondary_color, vi.temp_version,
		vi.certificate_version, vi.wvta_no, vi.manufacture_date, vi.issue_date, vi.json, vi.xml_template_id,
		vi.vehicle_template_id, vi.deleted, vi.create_by, vi.create_time, vi.update_by, vi.update_time, vi.remark,
		vi.validation_report_json, vi.engine_number, vi.motor_number, vi.brand, vi.weight, vi.sale_name, vi.tire,
		vi.battery_number, vi.tvv, vi.breakpoint_time, vi.generate_affirm, vi.upload_affirm, vi.project_name,
		vi.customer_no, vi.tire_resistance_grade, vi.first_material_flag, vi.first_template_flag,
		vi.material_confirmed_by, vi.material_confirmed_time, vi.template_confirmed_by, vi.template_confirmed_time,
		vi.generate_affirm_notice, vi.upload_affirm_notice, vi.sale_company_name, vi.model_no, vi.affirm_cause,
		sdd.dict_label AS vehicle_model_str,
		vt.filepath AS vehicle_template_filepath, vt.coc_template_no AS coc_template_no, vt.version AS version,
		IF(xf.id IS NOT NULL, TRUE, FALSE)  AS has_generated_xml,
		xf.id                               AS xml_file_id,
		xf.validate_result                  AS xml_validate_result,
		xf.force_uploaded                   AS force_uploaded
		FROM vehicle_info vi
		LEFT JOIN (SELECT dict_code, dict_label FROM sys_dict_data WHERE dict_type = 'vehicle_model') sdd
		ON vi.vehicle_model = sdd.dict_code
		LEFT JOIN (SELECT template_id, filepath, coc_template_no, is_last, version, model_no, tvv FROM vehicle_template) vt
		ON vi.vehicle_template_id = vt.template_id
		LEFT JOIN xml_file xf
		ON xf.vin = vi.vin AND xf.is_latest = 1 AND xf.deleted = 0
		<where>
			AND vi.deleted = 0
			<if test="vehicleInfo.affirm != null">
				<choose>
					<when test="dimension == 'material'">
						AND vi.first_material_flag = 1
						AND vi.generate_affirm = 0
					</when>
					<when test="dimension == 'template'">
						AND vi.first_template_flag = 1
						AND vi.upload_affirm = 0
					</when>
				</choose>
			</if>
			<choose>
				<when test="dimension == 'material'">
					AND vi.first_material_flag = 1
				</when>
				<when test="dimension == 'template'">
					AND vi.first_template_flag = 1
				</when>
			</choose>
			<!-- 以下复用通用查询条件，vehicleInfo 参数前缀为 vehicleInfo. -->
			<if test="vehicleInfo.vinList != null and vehicleInfo.vinList.size() > 0">
				AND (
				<foreach collection="vehicleInfo.vinList" item="v" separator=" OR ">
					vi.vin LIKE CONCAT('%', #{v}, '%')
				</foreach>
				)
			</if>
			<if test="vehicleInfo.vin != null and vehicleInfo.vin != ''">
				AND vi.vin LIKE CONCAT('%', #{vehicleInfo.vin}, '%')
			</if>
			<if test="vehicleInfo.materialNoList != null and vehicleInfo.materialNoList.size() > 0">
				AND (
				<foreach collection="vehicleInfo.materialNoList" item="m" separator=" OR ">
					vi.material_no LIKE CONCAT('%', #{m}, '%')
				</foreach>
				)
			</if>
			<if test="vehicleInfo.materialNo != null and vehicleInfo.materialNo != ''">
				AND vi.material_no LIKE CONCAT('%', #{vehicleInfo.materialNo}, '%')
			</if>
			<if test="vehicleInfo.vehicleModelList != null and vehicleInfo.vehicleModelList.size() > 0">
				AND (
				<foreach collection="vehicleInfo.vehicleModelList" item="m" separator=" OR ">
					vi.vehicle_model LIKE CONCAT('%', #{m}, '%')
				</foreach>
				)
			</if>
			<if test="vehicleInfo.factoryCode != null and vehicleInfo.factoryCode != ''">
				AND vi.factory_code = #{vehicleInfo.factoryCode}
			</if>
			<if test="vehicleInfo.factoryCodes != null and vehicleInfo.factoryCodes.size() > 0">
				and factory_code in
				<foreach item="factoryCode" collection="vehicleInfo.factoryCodes" open="(" separator="," close=")">
					#{factoryCode}
				</foreach>
			</if>
			<if test="vehicleInfo.factoryName != null and vehicleInfo.factoryName != ''">
				AND vi.factory_name LIKE CONCAT('%', #{vehicleInfo.factoryName}, '%')
			</if>
			<if test="vehicleInfo.country != null and vehicleInfo.country != ''">
				AND vi.country = #{vehicleInfo.country}
			</if>
			<if test="vehicleInfo.countries != null and vehicleInfo.countries.size() > 0">
				AND country in
				<foreach item="country" collection="vehicleInfo.countries" open="(" separator="," close=")">
					#{country}
				</foreach>
			</if>
			<if test="vehicleInfo.color != null and vehicleInfo.color != ''">
				AND vi.color LIKE CONCAT('%', #{vehicleInfo.color}, '%')
			</if>
			<if test="vehicleInfo.secondaryColor != null and vehicleInfo.secondaryColor != ''">
				AND vi.secondary_color LIKE CONCAT('%', #{vehicleInfo.secondaryColor}, '%')
			</if>
			<if test="vehicleInfo.wvtaNo != null and vehicleInfo.wvtaNo != ''">
				AND vi.wvta_no LIKE CONCAT('%', #{vehicleInfo.wvtaNo}, '%')
			</if>
			<if test="vehicleInfo.cocTemplateNo != null and vehicleInfo.cocTemplateNo != ''">
				AND vt.coc_template_no LIKE CONCAT('%', #{vehicleInfo.cocTemplateNo}, '%')
			</if>
			<if test="vehicleInfo.tvv != null and vehicleInfo.tvv != ''">
				AND vi.tvv LIKE CONCAT('%', #{vehicleInfo.tvv}, '%')
			</if>
			<if test="vehicleInfo.brand != null and vehicleInfo.brand != ''">
				AND vi.brand LIKE CONCAT('%', #{vehicleInfo.brand}, '%')
			</if>
			<if test="vehicleInfo.saleName != null and vehicleInfo.saleName != ''">
				AND vi.sale_name LIKE CONCAT('%', #{vehicleInfo.saleName}, '%')
			</if>
			<if test="vehicleInfo.engineNumber != null and vehicleInfo.engineNumber != ''">
				AND vi.engine_number LIKE CONCAT('%', #{vehicleInfo.engineNumber}, '%')
			</if>
			<if test="vehicleInfo.projectName != null and vehicleInfo.projectName != ''">
				AND vi.project_name LIKE CONCAT('%', #{vehicleInfo.projectName}, '%')
			</if>
			<if test="vehicleInfo.customerNo != null and vehicleInfo.customerNo != ''">
				AND vi.customer_no LIKE CONCAT('%', #{vehicleInfo.customerNo}, '%')
			</if>
			<if test="vehicleInfo.tireResistanceGrade != null and vehicleInfo.tireResistanceGrade != ''">
				AND vi.tire_resistance_grade LIKE CONCAT('%', #{vehicleInfo.tireResistanceGrade}, '%')
			</if>
			<if test="vehicleInfo.issueDateBeginTime != null">
				AND DATE(vi.issue_date) >= DATE(#{vehicleInfo.issueDateBeginTime})
			</if>
			<if test="vehicleInfo.issueDateEndTime != null">
				AND DATE(vi.issue_date) &lt;= DATE(#{vehicleInfo.issueDateEndTime})
			</if>
			<if test="vehicleInfo.manufactureBeginTime != null">
				AND vi.manufacture_date >= #{vehicleInfo.manufactureBeginTime}
			</if>
			<if test="vehicleInfo.manufactureEndTime != null">
				AND vi.manufacture_date &lt;= #{vehicleInfo.manufactureEndTime}
			</if>
			<if test="vehicleInfo.createdBeginTime != null">
				AND DATE(vi.create_time) >= DATE(#{vehicleInfo.createdBeginTime})
			</if>
			<if test="vehicleInfo.createdEndTime != null">
				AND DATE(vi.create_time) &lt;= DATE(#{vehicleInfo.createdEndTime})
			</if>
			<if test="vehicleInfo.breakpointTime != null">
				AND DATE(vi.breakpoint_time) = DATE(#{vehicleInfo.breakpointTime})
			</if>
		</where>
		ORDER BY vi.manufacture_date ASC
	</select>

	<!-- 通过 uuid 查询该模版组下所有关联的 vehicle_template_id -->
	<select id="findTemplateIdsByUuid" resultType="string">
		SELECT DISTINCT vi.vehicle_template_id
		FROM vehicle_info vi
				 INNER JOIN vehicle_template vt ON vt.template_id = vi.vehicle_template_id
		WHERE vt.uuid = #{uuid}
		  AND vi.deleted = 0
	</select>

	<select id="selectOldVersionAndNewVersion" resultType="java.util.HashMap">
		SELECT vi.vin, vt1.version AS oldVersion, vt2.version AS newVersion
		FROM vehicle_info vi
		LEFT JOIN vehicle_template vt1 ON vt1.template_id = vi.vehicle_template_id
		LEFT JOIN material m ON m.material_no = vi.material_no
		LEFT JOIN vehicle_template vt2 ON vt2.template_id = m.vehicle_template_id
		WHERE vi.deleted = 0 and vi.material_no IN
		<foreach collection="list" item="materialNo" open="(" separator="," close=")">
			#{materialNo}
		</foreach>
	</select>

	<!-- 1. 批量更新 generate_affirm_notice 状态 -->
	<update id="updateGenerateAffirmNotice">
		UPDATE vehicle_info
		SET generate_affirm_notice = #{status},
		update_time            = SYSDATE()
		WHERE vehicle_id IN
		<foreach collection="vehicleIds" item="vehicleId" open="(" separator="," close=")">
			#{vehicleId}
		</foreach>
	</update>

	<!-- 2. 批量更新 upload_affirm_notice 状态 -->
	<update id="updateUploadAffirmNotice">
		UPDATE vehicle_info
		SET upload_affirm_notice = #{status},
		update_time          = SYSDATE()
		WHERE vehicle_id IN
		<foreach collection="vehicleIds" item="vehicleId" open="(" separator="," close=")">
			#{vehicleId}
		</foreach>
	</update>

	<!-- 3. 查询需要发送"生成确认通知"的记录 -->
	<select id="listPendingGenerateAffirmNotice" resultMap="VehicleInfoResult">
		<include refid="selectVehicleInfoVo"/>
		WHERE vi.generate_affirm        = 0
		AND vi.generate_affirm_notice = 0
		AND vi.deleted                = 0
		AND EXISTS (
		SELECT 1
		FROM sys_dict_data sdd
		WHERE sdd.dict_type  = 'reminder_time'
		AND sdd.dict_sort  = 3
		AND sdd.dict_value > 0
		AND DATE_ADD(vi.create_time, INTERVAL CAST(sdd.dict_value / 1000 AS UNSIGNED) SECOND) &lt;= NOW()
		)
	</select>

	<!-- 4. 查询需要发送"上传确认通知"的记录 -->
	<select id="listPendingUploadAffirmNotice" resultMap="VehicleInfoResult">
		<include refid="selectVehicleInfoVo"/>
		WHERE vi.upload_affirm        = 0
		AND vi.upload_affirm_notice = 0
		AND vi.deleted              = 0
		AND EXISTS (
		SELECT 1
		FROM sys_dict_data sdd
		WHERE sdd.dict_type  = 'reminder_time'
		AND sdd.dict_sort  = 4
		AND sdd.dict_value > 0
		AND DATE_ADD(vi.create_time, INTERVAL CAST(sdd.dict_value / 1000 AS UNSIGNED) SECOND) &lt;= NOW()
		)
	</select>

	<!--
    物料号维度首台车列表（永久显示，不受确认状态影响）：
    first_material_flag=1，按 material_no 分组取制造日期最早一条
	-->
	<select id="listFirstMaterialUnconfirmedEarliest" resultMap="VehicleInfoResult">
		SELECT vi.vehicle_id, vi.vin, vi.factory_code, vi.factory_name, vi.material_no, vi.vehicle_model, vi.upload_status,
			   vi.validation_result, vi.status, vi.country, vi.color, vi.secondary_color, vi.temp_version, vi.certificate_version,
			   vi.wvta_no, vi.manufacture_date, vi.issue_date, vi.json, vi.xml_template_id, vi.vehicle_template_id, vi.deleted,
			   vi.create_by, vi.create_time, vi.update_by, vi.update_time, vi.remark, vi.validation_report_json, vi.engine_number,
			   vi.motor_number, vi.brand, vi.weight, vi.sale_name, vi.tire, vi.battery_number, vi.tvv, vi.breakpoint_time,
			   vi.generate_affirm, vi.upload_affirm, vi.project_name, vi.customer_no, vi.tire_resistance_grade,
			   vi.first_material_flag, vi.first_template_flag, vi.material_confirmed_by, vi.material_confirmed_time,
			   vi.template_confirmed_by, vi.template_confirmed_time, vi.generate_affirm_notice, vi.upload_affirm_notice,
			   vi.sale_company_name, vi.model_no, vi.affirm_cause, sdd.dict_label AS vehicle_model_str,
			   vt.filepath AS vehicle_template_filepath, vt.coc_template_no AS coc_template_no, vt.version AS version,
			   IF(xf.id IS NOT NULL, TRUE, FALSE) AS has_generated_xml,
			   xf.id                              AS xml_file_id,
			   xf.validate_result                 AS xml_validate_result,
			   xf.force_uploaded                  AS force_uploaded
		FROM (
				 SELECT *,
						ROW_NUMBER() OVER (
                   PARTITION BY material_no
                   ORDER BY manufacture_date ASC, vehicle_id ASC
               ) AS rn
				 FROM vehicle_info
				 WHERE first_material_flag = 1
				   AND generate_affirm = 0
				   AND deleted = 0
			 ) vi
				 LEFT JOIN (SELECT dict_code, dict_label FROM sys_dict_data WHERE dict_type = 'vehicle_model') sdd
						   ON vi.vehicle_model = sdd.dict_code
				 LEFT JOIN (SELECT template_id, filepath, coc_template_no, is_last, version, model_no, tvv FROM vehicle_template) vt
						   ON vi.vehicle_template_id = vt.template_id
				 LEFT JOIN xml_file xf
						   ON xf.vin = vi.vin AND xf.is_latest = 1 AND xf.deleted = 0
		WHERE vi.rn = 1
	</select>

	<!--
        模版维度首台车列表（永久显示，不受确认状态影响）：
        first_template_flag=1，按 vehicle_template_id 分组取制造日期最早一条
    -->
	<select id="listFirstTemplateUnconfirmedEarliest" resultMap="VehicleInfoResult">
		SELECT vi.vehicle_id, vi.vin, vi.factory_code, vi.factory_name, vi.material_no, vi.vehicle_model, vi.upload_status,
			   vi.validation_result, vi.status, vi.country, vi.color, vi.secondary_color, vi.temp_version, vi.certificate_version,
			   vi.wvta_no, vi.manufacture_date, vi.issue_date, vi.json, vi.xml_template_id, vi.vehicle_template_id, vi.deleted,
			   vi.create_by, vi.create_time, vi.update_by, vi.update_time, vi.remark, vi.validation_report_json, vi.engine_number,
			   vi.motor_number, vi.brand, vi.weight, vi.sale_name, vi.tire, vi.battery_number, vi.tvv, vi.breakpoint_time,
			   vi.generate_affirm, vi.upload_affirm, vi.project_name, vi.customer_no, vi.tire_resistance_grade,
			   vi.first_material_flag, vi.first_template_flag, vi.material_confirmed_by, vi.material_confirmed_time,
			   vi.template_confirmed_by, vi.template_confirmed_time, vi.generate_affirm_notice, vi.upload_affirm_notice,
			   vi.sale_company_name, vi.model_no, vi.affirm_cause, sdd.dict_label AS vehicle_model_str,
			   vt.filepath AS vehicle_template_filepath, vt.coc_template_no AS coc_template_no, vt.version AS version,
			   IF(xf.id IS NOT NULL, TRUE, FALSE) AS has_generated_xml,
			   xf.id                              AS xml_file_id,
			   xf.validate_result                 AS xml_validate_result,
			   xf.force_uploaded                  AS force_uploaded
		FROM (
				 SELECT *,
						ROW_NUMBER() OVER (
                   PARTITION BY vehicle_template_id
                   ORDER BY manufacture_date ASC, vehicle_id ASC
               ) AS rn
				 FROM vehicle_info
				 WHERE first_template_flag = 1
				   AND upload_affirm = 0
				   AND deleted = 0
			 ) vi
				 LEFT JOIN (SELECT dict_code, dict_label FROM sys_dict_data WHERE dict_type = 'vehicle_model') sdd
						   ON vi.vehicle_model = sdd.dict_code
				 LEFT JOIN (SELECT template_id, filepath, coc_template_no, is_last, version, model_no, tvv FROM vehicle_template) vt
						   ON vi.vehicle_template_id = vt.template_id
				 LEFT JOIN xml_file xf
						   ON xf.vin = vi.vin AND xf.is_latest = 1 AND xf.deleted = 0
		WHERE vi.rn = 1
	</select>

	<select id="checkVehicleInfoTimeoutGenerate" resultMap="VehicleInfoResult">
		SELECT vi.*
		FROM vehicle_info vi
		INNER JOIN (
		SELECT *
		FROM sys_dict_data
		WHERE dict_type = 'reminder_time' AND status = '0' AND dict_sort = 5
		) sdd ON 1 = 1
		WHERE vi.deleted = 0
		<if test="noticeStatus != null">
			and vi.generate_timeout_notice = #{noticeStatus}
		</if>
		AND vi.upload_status = 0
		AND TIMESTAMPDIFF(MICROSECOND, vi.create_time, NOW()) / 1000 &gt;= CAST(sdd.dict_value AS SIGNED)
	</select>
	<select id="findMaterialNosByTemplateId" resultType="string">
		SELECT DISTINCT material_no
		FROM vehicle_info
		WHERE vehicle_template_id = #{templateId}
		  AND deleted = 0
		  AND material_no IS NOT NULL
	</select>

	<update id="updateGenerateAffirmByMaterialNo">
		UPDATE vehicle_info
		SET generate_affirm = #{generateAffirm},
			update_time = SYSDATE()
		WHERE material_no = #{materialNo}
		  AND deleted = 0
	</update>

	<update id="updateUploadAffirmByTemplateId">
		UPDATE vehicle_info
		SET upload_affirm = #{uploadAffirm},
			update_time = SYSDATE()
		WHERE vehicle_template_id = #{templateId}
		  AND deleted = 0
	</update>

	<!-- 查询指定 material_no 下、vehicle_template_id 为空的车辆（回溯补关联用） -->
	<select id="selectByMaterialNoWithoutTemplate" resultMap="VehicleInfoResult">
		SELECT *
		FROM vehicle_info
		WHERE material_no = #{materialNo}
		  AND (vehicle_template_id IS NULL OR vehicle_template_id = '')
		  AND deleted = 0
	</select>

	<!-- 回溯补关联：仅更新模版相关字段 -->
	<update id="retroactiveLinkTemplate">
		UPDATE vehicle_info
		SET vehicle_template_id   = #{templateId},
			tvv                   = #{tvv},
			wvta_no               = #{wvtaNo},
			json                  = #{json},
			brand                 = #{brand},
			weight                = #{weight},
			sale_name             = #{saleName},
			tire                  = #{tire},
			tire_resistance_grade = #{tireResistanceGrade},
			vehicle_model         = #{vehicleModel},
			project_name          = #{projectName},
			update_time           = SYSDATE()
		WHERE vehicle_id = #{vehicleId}
		  AND deleted = 0
	</update>

	<!-- 按 VIN 更新车辆上传状态 -->
	<update id="updateUploadStatusByVin">
		UPDATE vehicle_info
		SET upload_status = #{uploadStatus},
			update_time   = NOW()
		WHERE vin = #{vin}
		  AND deleted = 0
	</update>

	<!-- 按物料号批量重置 upload_status（信息变更后需重新生成） -->
	<update id="resetUploadStatusByMaterialNo">
		UPDATE vehicle_info
		SET upload_status = 0,
			update_time   = SYSDATE()
		WHERE material_no = #{materialNo}
		  AND deleted = 0
		  AND upload_status > 0
		  AND generate_affirm = 0
	</update>

	<!-- 按模版ID批量重置 upload_status -->
	<update id="resetUploadStatusByTemplateId">
		UPDATE vehicle_info
		SET upload_status = 0,
			update_time   = SYSDATE()
		WHERE vehicle_template_id = #{templateId}
		  AND deleted = 0
		  AND upload_status > 0
		  AND upload_affirm = 0
	</update>
</mapper>

<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.xml.mapper.XmlTemplateMapper">

    <resultMap id="XmlTemplateResult" type="com.ruoyi.xml.domain.XmlTemplate">
        <id property="templateId" column="template_id"/>
        <result property="templateCode" column="template_code"/>
        <result property="templateName" column="template_name"/>
        <result property="modelDictCode" column="model_dict_code"/>
        <result property="status" column="status"/>
        <result property="remark" column="remark"/>
        <result property="createBy" column="create_by"/>
        <result property="createTime" column="create_time"/>
        <result property="updateBy" column="update_by"/>
        <result property="updateTime" column="update_time"/>
        <result property="deleted" column="deleted"/>
    </resultMap>

    <!-- 查询列表 -->
    <select id="selectTemplateList" resultMap="XmlTemplateResult">
        SELECT * FROM xml_template
        <where>
            deleted = 0
            <if test="templateCode != null and templateCode != ''">
                AND template_code = #{templateCode}
            </if>
            <if test="templateName != null and templateName != ''">
                AND template_name LIKE CONCAT('%', #{templateName}, '%')
            </if>
            <if test="modelDictCode != null">
                AND model_dict_code = #{modelDictCode}
            </if>
            <if test="status != null">
                AND status = #{status}
            </if>
        </where>
        ORDER BY create_time DESC
    </select>

    <!-- 根据ID查询 -->
    <select id="selectById" resultMap="XmlTemplateResult">
        SELECT * FROM xml_template
        WHERE template_id = #{templateId} AND deleted = 0
    </select>

    <!-- 插入 -->
    <insert id="insert" parameterType="com.ruoyi.xml.domain.XmlTemplate" useGeneratedKeys="true" keyProperty="templateId">
        INSERT INTO xml_template (
        template_code, template_name, model_dict_code, status, remark, create_by, create_time, deleted
        ) VALUES (
        #{templateCode}, #{templateName}, #{modelDictCode}, #{status}, #{remark},
        #{createBy}, #{createTime}, #{deleted}
        )
    </insert>

    <!-- 更新 -->
    <update id="updateById" parameterType="com.ruoyi.xml.domain.XmlTemplate">
        UPDATE xml_template
        <set>
            <if test="templateCode != null">template_code = #{templateCode},</if>
            <if test="templateName != null">template_name = #{templateName},</if>
            <if test="modelDictCode != null">model_dict_code = #{modelDictCode},</if>
            <if test="status != null">status = #{status},</if>
            <if test="remark != null">remark = #{remark},</if>
            <if test="updateBy != null">update_by = #{updateBy},</if>
            <if test="updateTime != null">update_time = #{updateTime}</if>
        </set>
        WHERE template_id = #{templateId} AND deleted = 0
    </update>

    <!-- 逻辑删除 -->
    <update id="deleteByIds">
        UPDATE xml_template
        SET deleted = 1
        WHERE template_id IN
        <foreach collection="templateIds" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
        AND deleted = 0
    </update>
</mapper>